# Telegram Bot с системой оплаты (портфолио)

Краткое описание
- Простая демо-админка и магазин курсов для Telegram: просмотр курсов, покупка (тестовая оплата), управление курсами и пользователями через панель администратора.

Особенности
- Админка: статистика, просмотр пользователей, бан/разбан, рассылки, создание/удаление курсов (с возможностью добавить обложку как файл или URL), настройки (символ валюты).
- Курсы: PDF / VIDEO / LINK (тип содержимого через enum `ContentType`), превью и отправка контента пользователям после покупки.
- Платежи работают в тестовом режиме (`pay_test` callback).

Требования
- Python 3.11+ (рекомендуется)

Установка (локально)
1. Создайте виртуальное окружение и активируйте его:
   - Windows: `python -m venv .venv` → `.\.venv\Scripts\activate`
   - macOS/Linux: `python -m venv .venv` → `source .venv/bin/activate`
2. Установите зависимости:
   ```bash
   pip install -r requirements.txt
   ```
3. Создайте `.env` в корне проекта и задайте обязательные переменные:
   - `BOT_TOKEN` — токен Telegram-бота
   - `ADMIN_ID` — Telegram ID администратора
   - `DB_NAME`, `DB_PASS`, `DB_PORT`, `DB_HOST`, `DB_USER` — настройки PostgreSQL (используется asyncpg)

Инициализация БД
- Для создания таблиц запустите:
  ```bash
  python create_database.py
  ```
  (скрипт сбросит и создаст таблицы по моделям)

Запуск бота
- Запустите:
  ```bash
  python main.py
  ```
- Подключитесь к боту в Telegram, отправьте `/start`, затем (под аккаунтом с `ADMIN_ID`) `/admin` для входа в админку.

Админ-функции (кратко)
- `/admin` — открыть админ-меню
- 📊 Статистика — пользователи, курсы, покупки, выручка (символ валюты из настроек)
- 👥 Пользователи — список, детали, бан/разбан, отправить личное сообщение
- 📚 Курсы — список, создать курс (пошагово: title → price → description → content type → content → обложка или skip → подтверждение), удалить курс
- 💰 Продажи — просмотр последних покупок
- ✉️ Рассылка — отправить сообщение всем пользователям (preview + confirmation)
- ⚙️ Настройки — изменить символ валюты

Примечания
- Платежи имитируются тестовой кнопкой: оплата подтверждается через `pay_test`.
- Для картинок курса поддерживаются file_id (загруженная картинка) и URL.

Лицензия
- MIT

---
Если нужно, могу дополнить README диаграммой процесса или примерами env-файла.
